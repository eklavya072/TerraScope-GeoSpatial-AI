# TensorFlow 2.16.1 is the last release shipping native macOS Apple Silicon (arm64) wheels
# and supports Keras 3 model configs like the one in models/resnet50_eurosat_ft.h5
tensorflow==2.16.1
streamlit>=1.0.0
plotly>=4.14.3
pandas>=1.2.0
numpy>=1.26.0,<2.0
Pillow>=8.0.0
scikit-learn>=0.24.0
seaborn>=0.11.0
matplotlib>=3.3.0
python-dotenv>=0.19.0
tqdm>=4.62.0
opencv-python>=4.5.0
tensorflow-datasets>=4.0.0
# Needed by train.py and tests/benchmark.py (EuroSAT loading from Hugging Face)
datasets>=3.0.0
# Needed by the change_detection/ research pipeline (georeferenced GeoTIFFs)
# 1.3.11 is the last line compatible with numpy 1.26 / TensorFlow 2.16.
rasterio==1.3.11
