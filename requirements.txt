# The site is static: plain HTML, CSS and JavaScript under web/, with the
# models running in the visitor's browser through onnxruntime-web from a CDN.
# It needs no Python at runtime and no server beyond a static file host.
#
# This file exists for the two build-time scripts:
#   python scripts/build_site_data.py   exports web/data/site.json
#   python -m pytest tests/             checks the committed artefacts
#
# The benchmark itself installs from pyproject.toml + uv.lock.
pytest>=8.0
