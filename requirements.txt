# TensorFlow 2.16.1 is the last release shipping native macOS Apple Silicon (arm64) wheels
# and supports Keras 3 model configs like the one in models/resnet50_eurosat_ft.h5
tensorflow==2.16.1
# app.py uses st.container(border=...) and st.button(type=...), both added in 1.28
streamlit>=1.28
plotly>=4.14.3
pandas>=1.2.0
numpy>=1.26.0,<2.0
Pillow>=8.0.0
seaborn>=0.11.0
matplotlib>=3.3.0
opencv-python>=4.5.0
# Needed by train_resnet50.py / finetune_resnet50.py (EuroSAT loading from Hugging Face)
datasets>=3.0.0
